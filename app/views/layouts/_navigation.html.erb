<nav class="navbar navbar-default navbar-custom">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <h1 class="text-primary"><strong>PostIt!</strong></h1>
  </div>

  <div class="collapse navbar-collapse" id="navbar-collapse-1">
    <ul id="navbar-links" class="nav navbar-nav navbar-right"> <!-- SHOW PILLS ON SM+ -->
      <li><%= link_to 'Home', root_path, class: 'text-muted' %></li>
      <li class="dropdown"><%= render 'layouts/admin_menu' if logged_in? && current_user.admin? %></li> <!-- AppHelper -->
      <li class="dropdown"><%= user_options_link %></li> <!-- AppHelper -->
      <li><%= login_link %></li> <!-- AppHelper -->
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>
