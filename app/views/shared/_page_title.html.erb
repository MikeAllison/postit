<div class="row">
  <div class="col-xs-1 col-sm-offset-2">
    <%= new_post_button unless @posts.empty? %> <!-- 'New Post' button hidden if 'shared/no_posts' displayed -->
  </div>
  <div class="col-sm-9">
    <div class="page-title pull-right">
      <div class="btn-group">
        <button type="button" class="btn btn-default btn-lg"><%= page_title %></button> <!-- AppHelper -->
        <button type="button" class="btn btn-default btn-lg dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
          <span class="caret"></span>
          <span class="sr-only">Toggle Dropdown</span>
        </button>
        <ul class="dropdown-menu" role="menu">
          <li><%= link_to 'All Posts', root_path %></li>
          <% if Category.any? # Show first divider if there are categories %>
            <li class="divider"></li>
          <% end %>
          <% Category.all.each do |category| %>
            <li><%= link_to category.name, category %></li>
          <% end %>
          <% if logged_in? # Show 'Add Category' link if logged in %>
            <li class="divider"></li>
            <li>
              <%= link_to new_category_path do %>
                <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>&nbsp;&nbsp;Add Category
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
