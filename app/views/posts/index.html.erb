<%= render 'shared/category_selection' %>

<%= render 'shared/no_posts' if @posts.empty? %>

<%= render @posts.limit(@items_per_page).offset(@offset) %>

<%= render 'shared/pagination_links.html.erb', obj: @posts %>
