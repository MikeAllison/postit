<%= render @post %>

<div class="row">
  <div class="col-xs-12">
    <h3><strong><%= "Comments (#{@post.comments_count})" %></strong></h3>
    <hr />
  </div>
</div>

<%= comments_form if logged_in? %> <!-- Shortcut for render 'comments/form' -->

<%= render @post.reload.comments.votes_created_desc %>
